[project]
name = "langgraph-agent"
version = "1.0.0"
description = "LangGraph 多 Agent 协作 Demo"
requires-python = ">=3.10"
dependencies = [
    "langchain>=0.3.0",
    "langchain-openai>=0.2.0",
    "langgraph>=0.2.0",
    "python-dotenv>=1.0.0",
]
